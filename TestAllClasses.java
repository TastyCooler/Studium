/*Klasse TestAllClasses
• Legen Sie eine Klasse TestAllClasses und in dieser eine statische Methode main an.
• Erzeugen Sie in der Methode main ein Objekt der Klasse Period und von diesem eine Kopie.
• Ändern Sie die Dauer dieser Kopie und lassen Sie sich den Inhalt beider Objekte anzeigen.
• Erzeugen Sie ein Objekt der Klasse PointInTime , legen Sie wieder eine Kopie an und ändern Sie diese.
• Erzeugen Sie anschließend mit den zuvor erzeugten Objekten zwei unterschiedliche Objekte der Klasse Date und
ändern Sie einen Termin. Überprüfen Sie die Attributwerte. */

public class TestAllClasses{


//public static main(String[] args){
  /*Period p = new Period(5);
  Period d = p.clone();
  d.change(4);
  System.out.println(p.toString());
  System.out.println(d.toString());

  PointInTime pt = new PointInTime(1998,55,19);
  PointInTime pd = pt.clone();
  pd.change(3);

  Date da = new Date("Mafi", pt) */
//}

}